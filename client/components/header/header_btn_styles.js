import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btnContainer: {
    width: 40,
    height: 40,
    borderRadius: 500,
    // backgroundColor: "#E3E7AF",
    justifyContent: "center",
    alignItems: "center",
    overflow: "hidden",
  },
  btnImage: {
    width: 40,
    height: 40,
  },
});

export default styles;
